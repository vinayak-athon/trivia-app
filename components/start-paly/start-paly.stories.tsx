import React from "react";
import { StartPlay } from "@Components";

export default {
  component: StartPlay,
  title: "StartPlay",
};

export const Default = () => <StartPlay caption="Start Play" />;
